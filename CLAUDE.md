# 이슈 갱신 시 지켜야 할 규칙

"핫이슈"/일일 갱신 요청을 받으면, 새 아이템을 웹 검색으로 채우기 전에 **해당 날짜(오늘)로 실제 보도된 소식이 있는지** 먼저 확인한다.

- 오늘 날짜로 새로 나온 소식이 있다면: 기존처럼 "오늘의 이슈"로 반영한다.
- 오늘 날짜로 새로 나온 소식이 없고, 최근(지난 1~2주) 소식만 있다면:
  - 없다고 얼버무리지 말고 사용자에게 "오늘자 새 이슈는 없다"고 먼저 확인해준다.
  - 사이트에 반영할 때는 "오늘의 이슈"처럼 오해할 수 있는 문구 대신 **"최근 이슈"**로 명확히 표기한다.
    - h1 문구: "오늘, ~" → "최근, ~"
    - eyebrow: "Daily ~ Buzz Radar" → "Recent ~ Buzz Radar"
    - 상단 안내: "오늘 새 이슈 없음 · 최근 화제 이슈로 구성 (YYYY.MM.DD 기준)"
    - meta-row: "생성: 날짜" → "최근 이슈 갱신: 날짜"
  - 이미 지난 며칠간 다룬 소식을 재구성/재배치한 것뿐이라면, 마치 새 이슈처럼 포장하지 않는다.

index.html / index-chatgpt.html / index-gemini.html 세 페이지 모두 동일한 기준으로 표기를 맞춘다.

# 검색 시 반드시 놓치지 말아야 할 카테고리

공식 발표/장애 소식만 검색하면 정작 가장 화제성 큰 뉴스를 놓친다 (2026-08-16, "살해계획을 챗GPT에 입력했다가 오픈AI 신고로 체포된 미국 남성" 사건을 놓쳐서 사용자가 직접 지적함). 매일 갱신 시 아래 세 카테고리를 각각 별도로 검색한다:

1. 공식 발표/업데이트/가격·정책 변경
2. 장애·접속불량 ("OO 다운")
3. **사람들이 클릭해볼 만한 극적·인간드라마·논란·사건·범죄 관련 이슈** — AI가 위험한 대화를 감지해 신고한 사건, AI가 사람을 해고한 사건, AI 관련 소송·사고, 유명인/기업 연루 사건 등. 이런 건 공식 보도자료가 아니라 일반 뉴스로만 나오므로 "[서비스명] 체포/신고/사건/논란/소송" 쿼리를 반드시 별도로 돌린다.

순위(buzz)를 매길 때도 극적·논란·사건성 이슈를 장애 다음으로 높게 잡는다 (공식 발표보다 우선).

# 제목(title) 작성 규칙

과장·왜곡·허위사실은 절대 금지, 사실은 그대로 유지하되 사람들이 누르고 싶어지는 클릭베이트성 표현을 최대한 활용한다:
- 직접 인용(" ")
- 구체적 숫자
- 의외성·반전("~인줄 알았더니", "알고보니", "이유는")
- 감정이 실린 단어(논란, 충격, 역풍, 공포 등)

예시: `"전 여친 죽이겠다" 챗GPT에 살해계획 남긴 미국 남성, 오픈AI 신고로 체포` (O)
피할 것: `챗GPT 사용자가 살인을 계획함` 같은 건조한 문구 (X)

summary도 "왜 많이 검색되는지/누가 검색하는지"가 드러나게 쓴다.

# 결과물을 사용자에게 보여줄 때 지켜야 할 규칙

index.html / index-chatgpt.html / index-gemini.html 같은 결과 페이지를 사용자에게 "보여달라"는 요청을 받으면, 파일을 그냥 전송(SendUserFile)하지 말고 **아티팩트(Artifact)로 게시**해서 미리보기 링크로 보여준다.

- 원본 HTML은 `<!DOCTYPE>`/`<html>`/`<head>`/`<body>` 전체 문서 구조이므로, 아티팩트로 게시하기 전에 해당 태그들을 제거하고 `<title>`·`<style>`·본문·`<script>`만 남긴 조각(fragment) 형태로 변환한다.
- 구글 폰트 등 외부 리소스 `<link>`는 아티팩트 샌드박스에서 차단되므로 제거한다(배포 사이트의 실제 폰트 로딩에는 영향 없음. 기존 CSS의 font-family 폴백 그대로 사용).
- 다크 전용으로 이미 디자인된 페이지이므로 라이트/다크 테마를 새로 만들 필요는 없고, 기존 색상·레이아웃을 그대로 보존한다(기존 디자인 시스템 존중).
- 클로드/챗GPT/제미나이 3개 페이지는 각각 별도의 아티팩트로 게시한다.
